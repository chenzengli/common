<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android"><!--layer-list不可少，下面的配置参数必须是带有小数的数值-->
    <item android:bottom="-3.50dp" android:left="-5.0dp" android:right="-5.0dp"
        android:top="0dp"><!--bottom表示从底部坐标开始，其它方向使用负数，表示从边界外指定位置开始绘制stroke线条，起到隐藏效果-->
        <shape android:shape="rectangle">
            <solid android:color="@color/white"/>
            <stroke android:width="1.0dp" android:color="@color/gray_c8c7cc"/>
        </shape>
    </item>
</layer-list>